# 实施路线

## 当前交付

- [x] 项目 README
- [x] 基础目录与忽略规则
- [x] 统一事件数据规范 v1（设计文档）
- [ ] 虚构样例与 ICS 生成
- [ ] 真实数据源
- [ ] 订阅地址与网站入口

## 第一步：已形成设计基线

见 [CalendarEvent 统一事件规范 v1](event-model.md)。已定义公共字段、分类扩展、稳定 UID、来源映射、日期与时区、显示模式、改期取消及 ICS 映射。此阶段没有业务代码。

## 下一步：第二步虚构样例

按照规范第 8 节准备离线样例，覆盖三类内容、两种模式、跨日时区、待定与取消。随后再实现校验和 ICS 生成，完成客户端验证后接入真实来源。

## 初版边界

按四款游戏、动漫、新游发售生成六类基础订阅源，每类 start / span 两种模式，共 12 个文件；路径契约见事件规范。任意组合订阅的实现方式另外设计，不能仅靠前端勾选就让静态服务器自动生成任意组合文件。

暂不实现账号系统、用户数据库、个性化 token、爬虫、API 接入或线上部署。

## 候选数据源（待核实）

| 类别 | 候选方向 | 需要核实 |
| --- | --- | --- |
| 游戏事件 | 官方公告、各游戏 Wiki、PRTS、HoYoLAB 等 | 服务器差异、历史数据、访问方式、许可与维护成本 |
| 动漫周更 | AniList、Bangumi 等 | 集数及播出时间覆盖、时区、延播和限流 |
| 新游戏发售 | IGDB、官方发售公告等 | 凭据要求、地区与平台差异、未定日期处理 |

## 后续验收重点

- ICS 可被目标客户端解析和订阅。
- UID 在改期与重复生成时稳定；客户端能正确更新事件。
- 全天事件结束日期、精确时间、跨日与时区处理符合所选语义。
- 数据源失败不会把有效日历覆盖为空。
- HTTPS 订阅 URL 稳定；客户端刷新延迟有明确说明。
